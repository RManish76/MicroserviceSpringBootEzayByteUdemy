We are going to implement IAM.
    IAM - OpenID Connect (OCID) + OAuth
        OCID - for id_token --> stores details of Resource owner(user). which is responsible for who are you?
        OAuth - for access_token --> stores the details of Authorization. what are you allowed to do?
                                        (what to allow to access other
                                        than profile detials like allow to access photos stored in google photos
                                        or allow to access the repo present in github repo account.)


Now to implement this we'll use KeyCloak to acheive the same. which will be responsible for generating the
    tokens and verifying the tokens.

    Gatewayserver will act as the service provider. Means any request which want to access any resource from
    our application will have to go thorugh the gatewayserver. And each request will have the id_token and
    access_token. Before provding the requested resource in the request the gatewayserver will reach to 
    KeyCloak to veirfy the resource and will provide the resource only once if tokens are vaild.

    Now the question is that its ok if request goes to gatewayserver but now in our case all MS endpoints
    are open to any one means anyone can trigger any rest endpoints of any MS without authentication or
    authorization.
    Yes but in real project all MS will be behind a firewall. And now can access anything which is inisde the
    firewall from outside of firewall. And the only entry point to firewall is available through the
    gatewayserver server. Hence no one will able to access any MS except with the gatewayserver endpoint.
    And also it means that we don't need to apply any authorization validation to any MS as all request will
    come thorugh only gatewayserver that request will be verified by gatewayserver and KeyCloak.

    You can also say that we apply the validation logic in all MS as well but it's bad practice also it'll
    make our MS to extra thing which it suposed it should not do. Also it'll send too many request to
    KeyCloak as one request can travel to multiple MS, and each MS will try to validate the request.


*1*  Now to download and run th KeyCloak we'll use docker and below command
    docker run -d -p 127.0.0.1:7080:8080 -e KC_BOOTSTRAP_ADMIN_USERNAME=admin -e KC_BOOTSTRAP_ADMIN_PASSWORD=admin quay.io/keycloak/keycloak:26.5.0 start-dev

    the KeyCloak runs by at port 8080 but the accounts MS is also running at port 8080.
    It fine if in docker internal it runs 8080 but for our local network we will redirect it to 7080 port
    And we can see in the command that we'll get defaul admin logic with username & password both as admin.

        In KeyCloak or any IAM service provider there is concept of realm.
        Bydefault we get master realm in KeyCloak, but we can setup other realm like qa, UAT, prod etc.

    We can goto http://localhost:7080/

    Registering a client from admin server from master realm
        Click Clients->Create client
                Client type: OpenID Connect --> means OpenID+oauth
                Client ID: eazybank-callcenter-cc  (custom, can be anything--> like username)
                Name: EazyBank Call Center App   (custome, can be anything, not mandatory)
                Description: EazyBank Call Center App   (custome, can be anything, not mandatory)

                NEXT

                Client authentication: Yes (authenticate the client with credentials then issue auth token.)

                Disable -- Standard Flow, Direct Access Grants
                Enable -- Service accounts roles   (flowq 'Client Credentials Grant' flow, don't know but feels like its for providding the token and validating the toke)

                NEXT -> NEXT-> SAVE

            Now to get the all endpoint related to KeyCloak, click on Realm settings at left side then scroll down
            (in General tab) at bottom setting click on OpenID Endpoint configuration.
            This will take you to a page (http://http://localhost:7080/admin/master/console/#/master/realm-settings)
             which contains all endpoints of KeyCloak.


    token api endpoint of KeyCloak: http://localhost:7080/realms/master/protocol/openid-connect/token
        body:  x-www-form-urlencoded 

                grant_type = client_credentials
                client_id = eazybank-callcenter-cc
                client_secret = -------get from the keycloak (it is the password)
                scope = openid email profile




*2* Updating GatewayServer

    Updating the gatewayserver pom dependency for spring security and oauth.
		<!-- Spring security dependency and oauth2 dependency -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-oauth2-resource-server</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-oauth2-jose</artifactId>

    Then created a folder with name config in root package where controller and filter foler is located.
    We'll create a file with name as SecurityConfig.java

        In this file added the details of api endpoint pattern for which spring need to look for the authentication
        and for which it does not look for authentication.
        Also we disabled the csrf protection which only required for browser based communication. If not disiabled
        then it'll cause failuer of POST(), PUT() and DELETe() request.


    Curretly the GatewayServer know which request it needs to authenticate and which not.
        But it does not know how to authenticate or validate.
        To do that we'll add a configuration in application.yml to tell spring to connect this for authentication.
            spring.security.oauth2.resourceserver.jwt.jwk-set-uri = "http://localhost:7080/realms/master/protocol/openid-connect/certs"

        The purpose of this path?
            During the statup of the resource server, it is going to connect with the resource server and it is going to
            download a public certificate from the KeyCloak server.
            purpose of this certificate?
                Using this public certificate, my resource server can validate if a given access token is really is used by
                the KeyCloak server.
                    If yes, is it a valid access token or an invalid access token?
                    So all those checks on the access token, it can perform with the help of this public certificate.
                The KeyCloak is going to have private certificates, Using the private certificates, the keycloak can only
                issue the new tokens, whereas with the help of this public certificate, any server like resource server can
                validate if a given access token is valid or not. They cannot really produce new tokens, but they should be able
                to validate if the given access token is valid or not.


    Now after doing all config out application knows how to authenticate a user using access token.
    But it knows nothing about authorization. Means Curretly if any user successfully logged/authenticated
    then he can trigger any api endpoint. where as we want to limit certain endpoint to only
    some particular user.

        This time instead of putting .authenticated in SecurityConfig.java we'll use .hasRole(roleName) as pass the
        role name which required for that pattern to user have.
        Now we have given required role to endpoint pattern like .hasRole("CARDS").
        But we need to configer these roles to our client application which auth server / KeyCloak

        Now we goto the KeyCloak and go to the user which we created, we'll see two tabs roles and 
        service account roles. Since our user is non-human or application then we should configure the roles
        in service account roles not in Roles tab.

        But to assign the roles we have to create the role so goto Realm roles and click on create roles
                Role Name: ACCOUNTS
                Description: Accounts role
            Do the same for cards and loans role.  //we leave for now for negative testing, latter will create it.
        
        No goto the client/user click on service account role the assign, then change fileter to filter by realm roles
        and select the required role.

        Now if we paste the new access_token to jwt decoder then we'll see new filed roles and list of assigned roles.


    Now since we are getting the roles in jwt access_token, we need to write some logic inside our resource server
    or inside the gatewayserver to extract the role information and convey the same to Spring Security framework
    to use and validate my authorization rules that I have configured.

    For the same, inside the same package of config we are creating new class with name KeycloakRoleConverter.java
        in this clas we'll extract the roles that we are getting from the KeyCloak into the format that Spring 
        Security framework can understand.

        int this class we implemented a interface which has a method convert. 
        We'll override this method. this method retur type is GrantedAuthority. When ever we want to talk
        about roles with spring security we need to send the details in GrantedAuthority format.

        Now we have fetched the role from the payload and converted it to GrantedAuthority and create the KeycloakRoleConverter.
        But we need to communicate about this role converter to the configurations inside the SecurityConfig class.

        for same